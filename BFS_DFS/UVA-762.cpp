#include<cstdio>
#include<cstring>

#define MAX 50

using namespace std;

int n;
string list[MAX][MAX];
int vis[MAX][MAX];

void DFS(int cur){

}

int main(){
	int i, j;
	char str[6];

	while(scanf("%d", &n) != EOF){
		for(i=0; i<n; ++i){
			scanf("%s", str);
			strncpy(list[]);
		}
	
	
	}


	return 0;
}
